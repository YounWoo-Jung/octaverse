# octaverse runtime configuration
# Set REQUIRE_REPO to auto-sync requirement markdown files from git.
# Example: REQUIRE_REPO=https://github.com/org/octaverse-require.git
REQUIRE_REPO=

# Daemon polling interval in seconds
SYNC_INTERVAL=60
